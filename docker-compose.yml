services:
  postgres:
    image: postgres:16
    environment:
      - POSTGRES_USER=test
      - POSTGRES_PASSWORD=test
      - POSTGRES_DB=testdb

  tests:
    build: .
    depends_on:
      - postgres
    environment:
      - BASE_URL=https://petstore.swagger.io/v2